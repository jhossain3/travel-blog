import * as React from "react";
import {Typography, Box }from '@mui/material';

export default function About() {
  return (


     <Box  item container  sx={{  display:'flex', justifyContent: "center", alignItems:'center', height: '600px', width:'100%'}} >
      <Typography sx={{}}> this is an about page cba to fill but ya get the point like</Typography>
      </Box>

  );
}
